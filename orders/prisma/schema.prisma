datasource db {
provider = "postgres"
  url      = "postgres://qfymckoixnrzrt:2dfb3bd475fbcd5a70355e2e89977687e1e463e33837ac39fe5347999e9d15c4@ec2-52-200-5-135.compute-1.amazonaws.com:5432/d8l97o6ve6b91c"
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id            String     @default(cuid()) @id
  username      String     @unique
  title         String
  name          String
  surname       String
  orders        Order[]
  @@map(name: "users")
}

model Order {
  id            String    @default(cuid()) @id
  createdAt     DateTime  @default(now()) @map(name: "created_at")
  updatedAt     DateTime  @updatedAt @map(name: "updated_at")
  products      String
  buyer         User?     @relation(fields: [buyerId], references: [id])
  buyerId       String?
}